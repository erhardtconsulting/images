name: "Init-Postgres: Build Container Image / Pull Request"

on:
  pull_request:
    branches:
      - main
    paths:
      - 'init-postgres/**'
      - '.github/workflows/build-init-postgres.yml'

permissions:
  contents: read
  packages: write

jobs:
  call-build-workflow:
    name: Build
    uses: erhardtconsulting/images/.github/workflows/build-container.yaml@main
    with:
      image: base
      version: "16"
      push: false